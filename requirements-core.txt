# Core CLI, report, storage, pipeline, and safety checks use the Python standard library.
# Keep this file free of dashboard dependencies so Python 3.15 can run core mode without
# building heavy packages such as streamlit, pyarrow, pandas, or numpy.
