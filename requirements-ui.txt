-r requirements-core.txt

# Streamlit currently pulls pyarrow/pandas wheels that may not be available on
# bleeding-edge Python releases. Keep Python 3.14/3.15 core mode installable by
# skipping the optional UI stack there.
streamlit>=1.30; python_version < "3.14"
reportlab>=4.0; python_version < "3.14"
