services:
  dashboard:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./reports:/app/reports
      - ./exports:/app/exports
      - ./logs:/app/logs
    command: ["streamlit", "run", "app/dashboard.py", "--server.address=0.0.0.0"]
